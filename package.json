{
  "name": "spec-intake",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Layer 1a: the only place a human touches the system. Human intent in, an authorised specification out.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "node bin/dev.ts",
    "serve": "node bin/serve.ts",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.72.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^4.4.3"
  }
}
